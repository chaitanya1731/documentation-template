# Root Readme

Note: 

If building using `_config.yml` jekyll theme -> Update Actions settings to build from `main` branch.

If building using sphinx framework and GHA -> Update Actions settings to build from `gh-pages` branch.
